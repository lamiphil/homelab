apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-library-local
  namespace: immich
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Ti
  storageClassName: immich-local-static
  volumeName: immich-library-local
